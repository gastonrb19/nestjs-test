services:
  postgres_nestjs:
    image: postgres:12.4-alpine
    container_name: pg_nestjs
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME}
    ports:
      - '5432:5432'
